# The lab runs offline by default (LLM_PROVIDER=echo) with no key required.
# Point it at a real provider below to run the same scenarios against a live
# model once the real-provider adapter lands (in progress).

LLM_PROVIDER=echo            # echo | openai | anthropic
# OPENAI_API_KEY=your_key_here
# ANTHROPIC_API_KEY=your_key_here

LAB_DB_PATH=lab_kb.json
